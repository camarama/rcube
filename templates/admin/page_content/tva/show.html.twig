{% extends 'admin/tableau_de_bord.html.twig' %}

{% block title %}Tva{% endblock %}

{% block page_content %}
    <h1>Tva</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tva.id }}</td>
            </tr>
            <tr>
                <th>Multiplicate</th>
                <td>{{ tva.multiplicate }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tva.nom }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ tva.valeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_tva_index') }}">back to list</a>

    <a href="{{ path('admin_tva_edit', {'id': tva.id}) }}">edit</a>

    {{ include('admin/page_content/tva/_delete_form.html.twig') }}
{% endblock %}
